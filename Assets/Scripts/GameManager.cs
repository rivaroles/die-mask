using UnityEngine;

public class GameManager : MonoBehaviour
{
    void Start()
    {
        var results = Random.Range(1, 7);
        Debug.Log(results);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
